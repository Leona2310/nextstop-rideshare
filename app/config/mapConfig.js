// Map tile provider configuration
// Choose provider: 'maptiler' | 'thunderforest' | 'osm'
// Replace the API key placeholders with real keys when available.

// Use MapTiler tiles (recommended). Get a free key at https://www.maptiler.com/
export const provider = 'maptiler';
export const MAPTILER_KEY = 'nF1Lu0Etyfy9JomY7Xc9';

// No other providers are used in this project. For production, do not commit API keys to the repo — load from secure storage or environment variables.
