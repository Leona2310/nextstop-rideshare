import { View } from 'react-native';

// Minimal stub kept for backward compatibility; prefer using MapComponent instead.
export default function MapPlaceholder() {
  return <View style={{ flex: 1 }} />;
}
